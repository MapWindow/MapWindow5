namespace MW5.UI.Repository
{
    public enum RepositoryCommand
    {
        AddFolder = 0,
        RemoveFolder = 1,
        RemoveFile = 2,
        AddToMap = 3,
        OpenLocation = 4,
    }

    public enum RepositoryItemType
    {
        FileSystem = 0,
        Folder = 1,
        Vector = 2,
    }
}
